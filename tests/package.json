{
	"name": "tests",
	"description": "Tests for @fourlights/mapper-plugin-anonymize",
	"private": true,
	"scripts": {
		"setup": "npm install > /dev/null && cd ../ && npm install > /dev/null && npm run build > /dev/null && cd -",
		"pretest": "npm run setup",
		"test": "jest",
		"pretest:fast": "npm run setup",
		"test:fast": "TEST_FAST=true jest"
	},
	"dependencies": {
		"@fourlights/mapper-plugin-anonymize": "file:../"
	},
	"devDependencies": {
		"jest": "^29.7.0",
		"ts-jest": "^29.1.2",
		"ts-node": "^10.9.2",
		"typescript": "^5.4.5"
	}
}
